#include "include/linkedlist_block.h"


void createNode_blok_jatuh (TetrisBlock* block, BlockNode* head, BlockNode* tail) {
{
    BlockNode* newNode = (BlockNode*)malloc(sizeof(BlockNode));
    if (newNode == NULL) {
        return;
    }
    

}